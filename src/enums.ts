export enum SortDirection {
  asc = 'asc',
  desc = 'desc',
}

export enum DocumentStatuses {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  RESOLVED = 'RESOLVED',
}

export enum Languages {
  ru = 'ru',
  en = 'en',
}
